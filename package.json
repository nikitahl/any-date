{
  "name": "any-date",
  "version": "1.0.0",
  "repository": "git@github.com:nikitahl/any-date.git",
  "author": "Nikita Hlopov",
  "license": "MIT",
  "scripts": {
    "start": "parcel index.html",
    "predeploy": "rm -rf dist && parcel build index.html --public-url ./",
    "deploy": "gh-pages -d dist"
  },
  "targets": {
    "default": {
      "publicUrl": "/any-date"
    }
  },
  "devDependencies": {
    "@babel/core": "^7.25.2",
    "@babel/preset-env": "^7.25.3",
    "eslint": "^9.8.0",
    "parcel": "^2.15.1"
  },
  "dependencies": {
    "gh-pages": "^5.0.0"
  }
}